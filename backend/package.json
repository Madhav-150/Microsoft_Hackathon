{
  "name": "phishshield-backend",
  "version": "1.0.0",
  "description": "Azure Functions Backend for PhishShield",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/ai-text-analytics": "^5.1.0",
    "@azure/functions": "^4.0.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "azure-functions-core-tools": "^4.6.0",
    "rimraf": "^5.0.0",
    "typescript": "^4.9.5"
  }
}
